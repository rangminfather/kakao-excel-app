# Changelog

## 1.5.0 - 2026-07-02

### 핵심 개선
- 대량 카카오톡 대화 파일 처리 안정성을 개선했습니다.
- 기본 처리 흐름을 로컬 우선 분석으로 강화해 AI API 의존도를 크게 낮췄습니다.
- 대량 파일로 판단되면 Gemini 2.5 Flash Lite를 자동 사용하도록 했습니다.
- 일반 파일은 Gemini 2.5 Flash를 기본 모델로 유지합니다.

### 엑셀 추출 품질 개선
- `텍스트 전체(AtoZ)` 처리 범위를 추가했습니다.
- 날짜가 바뀌면 엑셀에서 빈 행 2줄로 자동 구분합니다.
- 엑셀 1행 헤더를 틀 고정해 아래로 내려도 컬럼명을 계속 볼 수 있게 했습니다.
- 결과 화면은 전체 행을 모두 렌더링하지 않고 일부만 미리보기로 표시해 대량 결과에서도 앱이 버벅이지 않게 했습니다.
- `비고` 컬럼을 추가해 애매한 계산이나 추정 근거를 남기도록 했습니다.

### 파서 보강
- 단가 범위 표현을 안전하게 처리합니다.
  - 예: `참치액4.180~7.980×29=128.820`
  - 단가를 확정할 수 없으면 단가는 비워두고 검토 필요로 표시합니다.
  - 가능한 경우 `4,180원 27개 + 7,980원 2개 = 128,820원 (추정)`처럼 비고에 추정 근거를 남깁니다.
- 금액이 다음 줄에 적힌 보고를 처리합니다.
  - 예: `딤섬3종 9,980원×10개` 다음 줄 `=99,800원`
- 수량만 적힌 품목의 금액을 합계 차액으로 산출합니다.
  - 예: `왕교자 4990×78개`, `딤섬외6개`, `총 436,070원`
  - `딤섬외` 금액을 `46,850원`으로 계산하고 비고에 `합계 차액으로 산출`을 남깁니다.
- `1+1` 보고는 단순 `단가×수량`으로 확정하지 않고 수량 절반 기준 추정으로 처리합니다.
- 품목 없는 숫자 계산줄이 품목명으로 들어가는 문제를 수정했습니다.

### 저장 및 실행 흐름
- 저장 방식을 설정 화면이 아니라 추출 실행 직전에 선택할 수 있게 했습니다.
- 누적 저장, 새 파일 저장, 자동 저장 안 함을 실행 전 선택할 수 있습니다.
- 처리 중 경과 시간과 현재 단계를 표시하도록 개선했습니다.

### 오류 대응
- Gemini 503 또는 일시적 모델 거부가 발생해도 전체 작업이 실패하지 않도록 했습니다.
- AI가 필요한 일부 항목만 검토 행으로 남기고 로컬 분석 결과는 계속 저장합니다.
